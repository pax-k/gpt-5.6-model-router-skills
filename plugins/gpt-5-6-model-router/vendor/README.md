# Vendored dependency

`tomli/` contains Tomli 2.4.0 from
https://github.com/hukkin/tomli, bundled so the router can read TOML agent
templates on Python 3.9 and 3.10 without a network install.

The upstream MIT license is retained at `tomli/LICENSE`.
