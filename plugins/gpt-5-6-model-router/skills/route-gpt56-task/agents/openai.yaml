interface:
  display_name: "Route a GPT-5.6 Task"
  short_description: "Autonomously route work with evidence and review gates"
  default_prompt: "Use $route-gpt56-task to autonomously complete this work through the appropriate GPT-5.6 roles, making evidence-backed decisions without routine approval pauses."
policy:
  allow_implicit_invocation: false
