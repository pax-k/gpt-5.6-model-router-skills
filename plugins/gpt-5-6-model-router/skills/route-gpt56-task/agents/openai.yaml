interface:
  display_name: "Route a GPT-5.6 Task"
  short_description: "Governed GPT-5.6 routing"
  default_prompt: "Use $route-gpt56-task to handle this task with governed cost-aware routing, accountable overrides, and verified critical review."
policy:
  allow_implicit_invocation: false
