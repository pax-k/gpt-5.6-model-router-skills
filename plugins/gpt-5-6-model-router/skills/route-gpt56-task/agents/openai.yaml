interface:
  display_name: "Route a GPT-5.6 Task"
  short_description: "Autonomous GPT-5.6 routing"
  default_prompt: "Use $route-gpt56-task to handle this task with autonomous cost-aware routing, optional bounded delegation, and root override authority."
policy:
  allow_implicit_invocation: false
